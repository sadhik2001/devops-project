apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "payg-app.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "payg-app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
